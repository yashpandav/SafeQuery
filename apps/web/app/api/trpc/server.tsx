// tRPC server-side proxy removed — all procedures now live in apps/api (Express).
// RSC prefetching via createTRPCOptionsProxy will be re-added in a later phase
// once apps/api is deployed to a stable URL.
export {}
